package chess.engine.computer.eval;

import chess.engine.byteboard.Position;

final public class QueenEvaluator {

    // todo

    /** From white's perspective */
    public float evaluate(Position pos) {
        float score = 0;

        return score;
    }
}
